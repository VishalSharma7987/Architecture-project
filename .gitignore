# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — the Anthropic API key lives here and must never be committed.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Coverage reports — regenerated by `npm run coverage`, never reviewed as source.
coverage/

# Real architect drawings. Never committed — client-confidential, and the
# licence we ask for is engineering validation, not redistribution. Only the
# measured results (docs/testing/corpus-baseline.*) and a manifest of hashes
# go into the repo. See docs/testing/corpus-request.md.
corpus/
